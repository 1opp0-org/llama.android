# Specification: Core Library Refactoring

Refactor the library into `llama.android-core` (developer-maintained) and `llama.android-jni` (AI-maintained).
Establish project-wide namespaces and coordinate versioning via TOML.
